RSA Engine in C++

Prime Generation
-----------------------------
Generate two random bit streams p, q and call mpz_nextprime() 
According to GMP manual, it is adequete for practical purposes

n = mpz_mul(p,q), 
x = mpz_mul(p-1, q-1)

e = 65537
while(gcd(e,x) != 1)
	e += 2

d = mpz_invert(ROP, OP1, OP2)
	
